///@description Set up controls for the player
right = keyboard_check(vk_right);
left = keyboard_check(vk_right);
up = keyboard_check(vk_right);
down = keyboard_check(vk_right);

upRelease = keyboard_check_released(vk_up);